name: Functional Testing Pipeline
on: [push, pull_request]

jobs:
  logic_audit:
    name: Pytest Functional Audit
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r testing/requirements.txt

      - name: Run Pytest
        env:
          PYTHONPATH: GP2  
        run: pytest testing/test_server.py --junitxml=logic-report.xml

      - name: Upload Logic Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: functional-logic-results
          path: logic-report.xml

  api_audit:
    name: Postman API Audit
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          npm install -g newman
          pip install -r testing/requirements.txt

      - name: Start Server in Background
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: nohup python GP2/server.py > server.log 2>&1 &
        
      - name: Wait for Server (Health Check)
        run: |
          timeout 30s bash -c 'until curl -s localhost:5000; do echo "Waiting for server..."; sleep 2; done'
          echo "Server is UP and running!"

      - name: Run Postman Collection
        run: |
          newman run "testing/GP2 Raqeeb API Testing.postman_collection.json" \
          --environment "testing/GP2.postman_environment.json" \
          --env-var "base_url=http://localhost:5000" \
          --env-var "supabase_url=${{ secrets.SUPABASE_URL }}" \
          --env-var "supabase_key=${{ secrets.SUPABASE_KEY }}" \
          --timeout-request 5000 \
          --bail
          
      - name: Debug Server on Failure
        if: failure()
        run: cat server.log
